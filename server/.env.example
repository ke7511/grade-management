# 后端生产环境配置模板
# 在 Railway 部署时设置这些环境变量

# 服务器配置（Railway 会自动设置 PORT）
PORT=3000

# 数据库配置（从 Railway MySQL 服务获取）
DB_HOST=your-mysql-host.railway.app
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=railway

# JWT 密钥（请更改为复杂的随机字符串）
JWT_SECRET=change_this_to_a_very_long_random_string_in_production
JWT_EXPIRES_IN=7d
